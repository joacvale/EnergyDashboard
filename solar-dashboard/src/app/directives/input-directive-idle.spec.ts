import { InputDirectiveIdle } from './input-directive-idle';

describe('InputDirectiveIdle', () => {
  it('should create an instance', () => {
    const directive = new InputDirectiveIdle();
    expect(directive).toBeTruthy();
  });
});
